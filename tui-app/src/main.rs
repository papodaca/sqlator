// SQLator TUI — Future Ratatui terminal interface
// This crate will consume the same sqlator-core API as the Tauri desktop app.
fn main() {
    println!("SQLator TUI is not yet implemented. Use the desktop app for now.");
}
